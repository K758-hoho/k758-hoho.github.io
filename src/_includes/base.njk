<!DOCTYPE html>
<html lang="en">
<head>
  {% include "partials/head.njk" %}
</head>
<body>
  {% include "partials/navbar.njk" %} 
    <header>
        <h1>{{ title }}</h1>
        {% if tagline %}
          <div class="tagline">  
            <p>{{ tagline }}</p>
          </div>
        {% endif %}
        {% if tags %}
          <div class="tags">
            <ul>
                {% for tag in tags %}
                    <li>{{ tag }}</li>
                {% endfor %}
            </ul>
          </div>
        {% endif %}
    </header>

    <main>
      {{ content | safe }}
    </main>
  </div>

  {% include "partials/footer.njk" %}

</body>
</html>